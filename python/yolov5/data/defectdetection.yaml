# train and val datasets (image directory or *.txt file with image paths)
train: ./train.txt
val: ./valid.txt

# number of classes
nc: 4

# class names
names: ["photovoltaic_panels", "stripe_spot", "single_spot", "multi_spot"]